# lexiScan
“I built a complete end-to-end pipeline using spaCy, starting from manual BIO annotation of legal contracts, converting them into spaCy’s binary training format, training a custom NER model, and finally deploying it for inference on unseen legal text.”
